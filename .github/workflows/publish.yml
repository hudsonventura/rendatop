name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Git pull
        working-directory: /rendatop
        run: git pull




      - name: Docker build tests
        working-directory: /rendatop
        run: docker compose -f docker-compose-teste.yml build tests

      - name: Run pipeline tests
        working-directory: /rendatop
        run: docker compose -f docker-compose-teste.yml up --abort-on-container-exit --exit-code-from tests tests

      - name: Show test logs on failure
        if: failure()
        working-directory: /rendatop
        run: docker compose -f docker-compose-teste.yml logs

      - name: Shutdown test stack
        if: always()
        working-directory: /rendatop
        run: docker compose -f docker-compose-teste.yml down -v



      - name: Docker build
        working-directory: /rendatop
        run: docker compose build


      - name: Apply migrations
        working-directory: /rendatop
        run: docker compose up migrator


      - name: Restart compose
        working-directory: /rendatop
        run: docker compose down && docker compose up -d
